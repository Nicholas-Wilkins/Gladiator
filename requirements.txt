python-chess>=1.999
numpy>=1.24
rich>=13.0

# PyTorch is required for the NN engine (gladiator_nn).
# Install the correct build for your hardware — do NOT pip install torch directly
# from this file, as the right index URL depends on your GPU:
#
#   NVIDIA GPU (CUDA):   https://pytorch.org/get-started/locally/
#   AMD GPU on Linux (ROCm):
#     pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
#   AMD GPU on Windows (DirectML):
#     pip install torch torch-directml
#   CPU only:
#     pip install torch
